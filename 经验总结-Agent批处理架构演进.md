# 当上下文管理成为问题本身——一个 Agent 批处理项目的架构演进

## 项目背景

将门店不规范的商品数据（Excel），通过 AI 匹配到总部标准商品库，或参考总部范本进行标准化重命名/补全字段。典型场景：每个 Excel 包含数千行商品数据，每行需要独立处理。

技术栈：DeepSeek V4 Flash + LangGraph（langchain `create_agent`）+ Qdrant 向量库 + BGE 嵌入/重排。

---

## 第一幕：LLM + RAG，代码控制逻辑（RAGmatch）

在引入 Agent 之前，项目用的是传统的 LLM + RAG 管线。代码控制所有步骤，LLM 只在最后一步出场做裁判或生成。

### 匹配管线（Matcher）：条码 → 向量 → 重排 → LLM 批量裁判

```
门店商品
  ├─ 0. 条码精确匹配（Qdrant payload filter）
  │    命中 → 直接返回，跳过后续
  ├─ 1. 向量检索 top-15（BGE 嵌入商品名）
  ├─ 2. Reranker 重排 top-5（BGE Reranker）
  └─ 3. LLM 批量裁判（一次调用判 5 对，输出 match + 置信度 + 理由）
```

### 标准化管线（Normalizer）：向量检索范本 → 类目过滤 → LLM 参考范本生成

```
门店品名
  ├─ 1. 向量检索 top-30 个总部标准商品（RAG 范本）
  ├─ 2. 按通用类目.csv 过滤，取前 5 个合法范本
  └─ 3. LLM 一次性输出：标准化品名、售卖规格、前台类目、重量、单位
```

### Prompt 的两难困境

这个阶段遇到了一个棘手的问题：**prompt 怎么写都不对。**

- **写得太少**（几十字，"参考范本标准化这个商品"）→ LLM 输出随意，格式不统一，字段常常缺失，商品名瞎编
- **写得太多**（200+ 字，包含命名规范、规格格式、单位换算规则、类目约束等 15+ 条规则）→ LLM 进入"规则合规模式"，输出保守、刻板，商品名缺乏泛化。尤其商品名称——不是跟范本机械对齐丢失了品牌/型号信息，就是把描述词删掉了

这个困境是 LLM-only 模式的根本局限：

```
写得少                   写得多
  │                        │
  ▼                        ▼
约束不足                 认知过载
LLM 随意发挥             LLM 倾向保守
输出不一致               输出缺乏泛化
  │                        │
  └────────┬───────────────┘
           │
           ▼
    没有"刚好"的中间点
    因为所有约束必须同时挤压进一次调用
```

**这是推动向 Agent 模式转型的根本原因。** 不是因为 LLM-only 不够快或不够便宜，而是 prompt 设计存在一个无法跨越的鸿沟——规则要么太少要么太多，没有一个"刚好"的量能让 LLM 既准确又灵活。

---

## 第二幕：Agent 模式探索（AutoItemCreator）

将 LLM-only 改为 Agent 模式后，system prompt 从 200+ 字压缩到不足 100 字：

```
你是商超商品标准化专家。门店商品命名不规范，请参考总部范本和联网搜索进行标准化。
商品名称需要符合总部命名风格。其他字段请结合范本和联网搜索填充。
最后再检查一下各字段是否符合上述要求。
```

**效果立竿见影——商品名写对了，字段也补全了。但成本的代价是每行从 1 次 LLM 调用变成 3-6 次。**

更重要的是，Agent 模式开启了关于上下文管理的完整探索。从最初想做一个"能自己读 Excel、自己处理、自己写结果的智能 Agent"，到最终收敛为"Python 控制批处理，Agent 只负责单行推理"，每一步都是对 Agent 工作机制的一次重新理解。

### Agent 阶段 1：让 Agent 管一切——读文件、处理、写文件

最初的设想很直觉：给 Agent 装上读写 Excel 的工具，让它自己决定怎么处理。

```
工具列表：
  - read_excel(file_path)      → 读取整个文件
  - write_excel(data, path)    → 写入结果
  - search_products(query, ...) → 搜索总部商品
  - web_search(query, ...)     → 联网搜索
  - ...

Agent 拿到任务："把这个文件里的商品都标准化"
预期：Agent 调用 read_excel → 逐行处理 → 调用 write_excel
```

**结果：完全行不通。** 一个 Excel 有几千行，Agent 读完文件后上下文直接爆炸。即使勉强读完，后续处理时 LLM 要在几千行的数据中找到"当前该处理哪一行"，这远远超出了 LLM 的有效注意力范围。

---

### Agent 阶段 2：分批读取——引入进度控制

意识到单次读取行不通，改成让 Agent 分批读取：

```python
def read_batch(file_path, start_row, batch_size):
    """从第 start_row 行开始读 batch_size 行"""
    ...
```

**但马上发现了一个关键问题：Agent 会死循环。**

如果不显式告诉 Agent "你读到了什么位置、还剩多少、是否已经读完"，Agent 会反复调用 `read_batch`——即使文件已经读完了，它不知道，还会从第 0 行重新读起。

**关键洞察：必须把进度信息显式返回给 Agent。**

```python
# ❌ 只返回数据——Agent 不知道自己的位置，会死循环
def read_batch(file_path, start_row, batch_size):
    return {"data": rows}

# ✅ 返回完整的进度上下文
def read_batch(file_path, start_row, batch_size):
    return {
        "data": rows,
        "current_row": start_row,
        "read_count": len(rows),
        "total_rows": 5000,
        "remaining": 5000 - (start_row + len(rows)),
        "is_done": False,
        "progress": "已读 100/5000 行 (2%)",
    }
```

这个发现的意义比表面看起来更深——**LLM 不像人，它没有"我刚才做了什么"的直觉。它完全依赖上下文中显式存在的信息来理解当前状态。** 任何你期望 Agent "自然知道"的信息，如果不在上下文里，它就不会知道。

---

### Agent 阶段 3：上下文膨胀——尝试用中间件压缩

分批读取解决了单次读取的爆炸问题，但带来了新问题：随着处理推进，Agent 的上下文（消息历史）中积累了每一批的读取记录、处理结果、工具调用返回。处理到第 500 行时，前 499 行的所有历史都在上下文里。

于是尝试用 LangGraph 的中间件机制做上下文压缩：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  read_batch  │ ──► │  中间件压缩   │ ──► │  process     │
│  (第 1 批)   │     │  (摘要旧消息)  │     │  (处理商品)  │
└──────────────┘     └──────────────┘     └──────────────┘
                                                 │
                     ┌──────────────┐            │
                     │  中间件压缩   │ ◄──────────┘
                     │  (摘要旧消息)  │
                     └──────────────┘
```

**这个方法也失败了，而且失败的方式非常耐人寻味。**

---

### Agent 阶段 4：核心问题的暴露——Agent 的执行模式与压缩互斥

经过观察日志，发现了一个根本性的问题：**Agent 处理一批数据时，执行模式是"工具并发"而非"逐行完成"。**

```
我想象的执行顺序（逐行完成）：
  行 1: read → search → 输出 → 写入 ✓
  行 2: read → search → 输出 → 写入 ✓
  行 3: read → search → 输出 → 写入 ✓

Agent 实际的执行顺序（按工具并发）：
  read_batch(第 1 批，50行)
    ↓
  search_products(行1) + search_products(行2) + ...  ← 并发搜 50 行
    ↓
  search_by_barcode(行1) + search_by_barcode(行2) + ... ← 并发查 50 行
    ↓
  web_search(行1) + web_search(行2) + ...  ← 并发搜 50 行
    ↓
  ... LLM 尝试一次性生成 50 行的结果 ...
```

Agent 的并发工具调用机制让它倾向于"把一批数据的所有行的同一个工具一起调完"。这意味着：

1. **没有一行是完整处理完的。** 每一行都处于"部分完成"的状态——搜了但还没综合，或者综合了但还没校验。

2. **中间件压缩时，LLM 会摘要掉"看起来不完整的"中间状态。** 行 17 的搜索结果还没被使用就被压缩成了一句话"行 17 也搜过了"——具体搜到了什么丢了。

3. **压缩后的上下文，LLM 无法继续处理任何一行。** 每行都缺信息，无法产出最终结果。

4. **Agent 开始"失忆"——** 它意识到结果不完整，于是重新搜索。但此时压缩后的上下文已经把最初的批处理目标（"处理这 50 行商品"）稀释成了模糊的语义片段。它不知道哪些行已经搜过、哪些还没开始、每行的中间结果在哪。

形成恶性循环：**上下文膨胀 → 压缩 → 信息丢失 → 重复调用工具 → 更膨胀 → 再压缩 → 更多丢失**。

**关键洞察：上下文压缩的假设前提是"消息序列中，早期的消息对当前决策不再关键"。** 这在对话场景中大致成立——10 轮前的"你好"对当前问题通常不重要。但在批处理场景中，每一行的中间结果对于产生该行的最终输出都是关键的——压缩等于是把未完成的工作的中间状态抹掉了。

---

### Agent 阶段 5：最终方案——放弃在 Agent 内管理上下文

既然问题的根源是 **Agent 内部无法保证"逐行完成后再处理下一行"**，那么解决方案就是把这个保证放到 Agent 外部：

```
┌───────────────────────────────────────────────────────────┐
│  批处理层（纯 Python，控制每行生命周期）                    │
│                                                           │
│  for 每一行 in Excel:                                     │
│      agent = 新建独立Agent(thread_id=f"item-{i}")         │
│      result = agent.invoke(prompt=f"处理这一个商品: {row}")│
│      save(result)                                         │
│      agent 销毁 ← 上下文清零 ← 无累积                      │
│                                                           │
│  - ThreadPoolExecutor 控制并发度                           │
│  - 失败重试（指数退避）                                    │
│  - 行间完全隔离，没有共享 State                            │
└───────────────────────────────────────────────────────────┘
```

**核心思想：不是在 Agent 内部"管理"上下文使它不膨胀，而是从外部"消除"上下文积累的可能性。**

每行一个独立的 Agent 实例 + 独立的 `thread_id`，处理完即销毁。Agent 永远只处理一件商品——它的上下文里只有这件商品的信息、搜索工具的返回、以及自己的推理过程。处理完就没了。下一行从头开始。

**这从根本上解决了上下文压缩的所有问题：**
- 不需要压缩（开始就很小，结束就销毁）
- 不会失忆（每个 Agent 只管一行，不需要"记住"其他行）
- 不会出现中间状态丢失（每行要么完成、要么失败，没有"部分完成"）

这个方案成立还有一层前提：**行与行是互斥关系，而非累积关系**——第 N 行的处理不依赖第 N-1 行的任何结果，所以"每行一个 Agent"是无损的。如果任务改成后行要引用前行的结论（批内去重、跨行聚合），行级隔离就不再成立，上下文管理才会真正成为问题。回头看，之前做的压缩、摘要、滑动窗口都是在错误的前提下优化——**上下文管理的最高境界，是让上下文根本不需要被管理。**

分工也就此定型：**框架管内部，代码管外部。** LangGraph 的 State/Checkpoint/Thread 在行级隔离场景下价值有限——`ThreadPoolExecutor` + 每行独立 `thread_id` 就够用；但在单行 Agent 内部，ReAct 循环、并发工具调用、消息 Reducer 这些 LangGraph 能力发挥着核心作用。关键的架构决策不是选哪个框架，而是认清框架能力的边界。

---

## 第三幕：Token 经济学——把账单当架构输入

12000 行跑批实付 ¥80-100（同样的类目表若走工具方案，测算要 ¥160-180），这个阶段的架构决策都由账单驱动。为了把类目表的 token 账打下来，先试了两条看似更省的路，都败了，最后落在两招上：静态数据放对位置省单价，重跑的行不再付费。

### Token 阶段 1：类目分级加载——省 3500 tok 的诱惑，赌错检索路径

把全表换成轻量工具：Agent 先判断商品属于哪个一级，再只拉该一级下的二级（几十行、约 200 tok），看似能把 3700 压到 200。

推演时发现死角：**只有先猜对一级，才看得到正确的二级**。猜错时没有任何纠错信号——它不知道正确答案藏在哪个没查过的一级里，理性策略就是查一两次失败后放弃走兜底。这会把"类目建议"的触发率推高，恰好与收紧兜底门槛的目标背道而驰。

> 分级加载省的是 token，赌的是检索路径的正确性。**知识需要全局比对才能决策时，按需检索的"省"是虚假的节约。**

### Token 阶段 2：分类器封装成子 Agent——账面省 45%，丢掉检索能力

架构上更进一步：把"分类"本身封装成子 Agent 工具——子 Agent 的 system 只放一级类目列表（180 tok），挂载按需加载二级，主 Agent 检索完范本后调用它拿结论。token 账面很漂亮，单行成本预计省 45%。

但冒烟测试立即暴露要害——**子 Agent 丢失了检索能力，分类准度掉了**。实测"娃哈哈 AD钙奶 + 范本线索：乳酸菌"，子 Agent 圈对了"牛奶酸奶"一级，二级却跳去"养乐多"：范本线索只能通过参数以一行文本传入，替代不了主 Agent 那种"自己检索、看到完整范本、再在全表里比对"的信息闭环。

> **决策依赖检索证据的任务，不能把检索和决策拆开封装**——中间靠参数传话的信息衰减，不是省下的 token 能买回来的。

### Token 阶段 3：类目表进 system——钱、速度、全局视野三赢

两条捷径败退后换了思路：不动内容，动位置。类目表最初做成工具，Agent 每行调用一次拉全表，复盘账单时发现三重浪费：

**浪费一：token 付全价。** 前缀缓存只认从第一个 token 起完全一致的前缀。工具返回 append 在对话**中部**——位置在每行不同的用户消息之后，缓存到这里就断了，3700 tok × 每行 × 全价。放进 system_prompt 尾部则成为每行一致的固定前缀，吃到缓存价（约 1/10）。

**浪费二：多一轮往返。** 模型要先生成调用请求，工具返回后还要重新读一遍 3700 tok 并消化，每行多花 7-15s——万行批处理是十小时级。进 system 后整个蒸发。

**浪费三：全局视野时有时无。** 工具方案下类目表在调用之后才进上下文——检索范本时模型还看不到白名单，检索和分类用的是两份信息。进 system 后两步始终基于同一份全局视野。

```
工具方案（每行）:   [system 120] [行字段] [调工具请求] [类目表 3700 ←全价] → 判断
system 方案（每行）: [system + 类目表 3800 ←缓存价] [行字段] → 判断
```

**边界：只适用于静态数据。** 类目表、字段规范这类行行相同的常驻知识，进 system 完胜；`search_products`（结果取决于商品）、`web_search`（运行时才知道搜什么）这类因行而异的动态知识，工具按需拉取不可替代。心智模型：**system 是常驻字典，tools 是按需查的档案馆**。

> **为什么同样的内容，放 system 和放工具返回，成本差一个数量级？** 前缀缓存的匹配单位是 token 序列开头，与语义无关。工具返回天生在对话中部，这个位置决定了它永远吃不到缓存。评估"该不该做成工具"除了功能考量，还有位置经济学。

---

### Token 阶段 4：结果复用缓存——捷径修在编排层，错误从散布变聚簇

类目表的位置账结清之后，剩下的浪费在"重复付费"：同批数据重跑全部重新调 Agent、跨批次的相似商品重复推理。于是加了结果复用层：每行调 Agent 之前先查缓存（精确 → 向量 → Agent 三层瀑布），命中即把缓存里的结果当作 LLM 的输出注入后续流程。

**决策一：缓存查询放在编排层，不做 Agent 工具。** 做成工具的浪费与阶段 3 同构：工具返回在对话中部吃不到前缀缓存、多一轮往返。更关键的是缓存命中要求**确定性**——同一行重跑必须命中，这件事不能交给概率性的 Agent 决策。命中结果直接伪装成 LLM 的输出，走与 Agent 完全相同的后续流程：两条路径行为零分裂，只维护一份代码。

**决策二：两层检索，职责不同。** 精确层（编码等值匹配）服务**断点续跑**——同批数据重跑时认出"这行跑过"，key 只取行的内容字段、绝不含行号/文件名（续传的命根是同一行重跑出同一个 key）；向量层（相似度过阈值才复用）服务**跨批次泛化**——新批里有旧批处理过的相似商品。精确层免费且零风险永远排第一，向量层付一次嵌入费赌一次相似判断排第二。阈值不给默认值——同发现 3 的原则，业务决策必须逼着配置者亲手定。

**决策三：写入纪律。** 只存 LLM 原始输出（合并结果混着当行输入，换一行 key 就对不上）；缓存命中的行不回写（避免一次相似误判被固化成精确事实）；失败行永不入缓存（否则重跑直接命中空串，失败被焊死）；逐行成功即写而非批末统一写（跑到一半崩了，已完成的行已经在库里）。

**错误经济学的变化：从散布到聚簇。** ~1.7% 错误率可接受的前提是错误独立散布——每行单独错、单独改。入缓存后，同一个 key 的错误被后续所有行**继承**，重跑也不会自愈。缓存省的是"重跑的钱"，不是"纠错的钱"——想重判只能作废缓存。应对全是可见性工程：命中日志带分数、收尾报来源统计。

**成本账：近乎免费的保险。** 未命中路径新增 2 次嵌入 ≈ 单行 Agent 成本的 0.4%，整批不到 ¥0.5；盈亏平衡命中率 0.4%。断点续跑场景（8000 行崩了重跑）直接省约 ¥60——复用是设计目的，续跑是隐藏的主收益。

> **为什么捷径要修在编排层而不是做成 Agent 工具？** 确定性复用放在确定性层，概率性推理留给 Agent 层——缓存命中本身不需要任何语义判断，就不该消耗任何概率预算。同时缓存改变了成本结构也改变了错误结构：**它让错误从"独立的、可自愈的散点"变成"持久的、会传染的聚簇"——上缓存的那一刻，质量防线就要从"模型准不准"前移到"缓存干不干净"。**

---

## 配套发现

三幕主线之外积累的独立经验。

### 发现 1：详细 Prompt 反而降低输出质量

LLM-only 模式中，prompt 写了 200+ 字，涵盖命名规范、规格格式、单位换算、类目约束等 15+ 条规则。结果输出保守、刻板——规则过多时 LLM 进入"规则合规模式"压制了"语义理解模式"。

切换到 Agent 模式后，system prompt 压缩到不足 100 字，只给方向不给规则。约束通过工具返回的范本和类目列表自然体现，LLM 在低认知负荷下输出的灵活性和准确性都显著提升。

> **为什么 Prompt 不是越详细越好？** Prompt 中的每条规则都在和核心任务争夺 LLM 的注意力。对于需要语义理解和风格模仿的任务，过多的显式规则将 LLM 推入"规则匹配"模式。Agent 的优势在于将规则隐式化——约束通过工具返回的结构化数据自然体现，而不是靠 prompt 文本灌输。

### 发现 2：Agent 的多步调用本质是认知负荷拆分

Agent 每行 3-6 次 LLM 调用，但每次调取的 token 远低于单次复杂 prompt。成本买到的不是工具调用本身，而是 LLM 在每一步都能轻装上阵的认知空间。

> **什么时候用 Agent vs LLM-only？** 判断标准不是任务复杂度，而是认知负荷能否被拆分。任务需要同时权衡多个约束条件 → Agent；任务是简单的映射/分类/生成没有中间决策 → LLM-only 足够。

### 发现 3：工具的默认参数对 LLM 是陷阱

向量搜索工具最初有完整默认值（`limit=3`、`recall=10`）。结果是 LLM 偷懒用默认值，拿到 3 条结果后发现不够才在第二次传 `limit=30`。去掉所有默认值后，Agent 第一次就传了正确的参数。

**根因：** 人类把默认值理解为"可覆写的建议值"，LLM 把默认值理解为"开发者推荐，应该用这个值"。

> **对人类友好的 API 设计（默认值、开箱即用）对 LLM 是反模式。** 人类需要降低调用复杂度，而 LLM 需要明确的选择压力来做出正确决策。Agent 工具的 API 设计原则：让 LLM 感到"我必须做出选择"而非"我可以接受默认"。

### 发现 4：并发工具调用是隐藏的性能红利

Agent 在同一轮推理中会将不互相依赖的工具调用自动合并为并行执行（条码搜索 + 向量搜索 + 联网搜索 + 类目查询同时发出），单行处理时间明显缩短。这是 LangGraph ReAct 图自动处理的行为，不需要额外编码。

---

## 最终架构

```
┌───────────────────────────────────────────────────────────┐
│  批处理层（纯 Python）                                     │
│  - pandas 读 Excel                                        │
│  - 每行 invoke 前查结果复用缓存（精确→向量），命中即出      │
│  - ThreadPoolExecutor 控制并发数（4 线程）                  │
│  - 每行失败重试 3 次，指数退避                              │
│  - 行间完全隔离，无共享 State，无 Checkpoint               │
│  - Agent 成功后结果即写入复用缓存（逐行即写）              │
│  - 进度日志（含缓存来源）+ 结果收集 + pandas 写 Excel      │
│                                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  Agent #1   │  │  Agent #2   │  │  Agent #3   │       │
│  │  thread_id  │  │  thread_id  │  │  thread_id  │       │
│  │  =item-1    │  │  =item-2    │  │  =item-3    │       │
│  │             │  │             │  │             │       │
│  │  LLM 推理   │  │  LLM 推理   │  │  LLM 推理   │       │
│  │  ├─ tool 1  │  │  ├─ tool 1  │  │  ├─ tool 1  │       │
│  │  ├─ tool 2  │──│──├─ tool 2  │──│──├─ tool 2  │  并发  │
│  │  ├─ tool 3  │  │  ├─ tool 3  │  │  ├─ tool 3  │       │
│  │  └─ tool 4  │  │  └─ tool 4  │  │  └─ tool 4  │       │
│  │  (同一轮内   │  │  (同一轮内   │  │  (同一轮内   │       │
│  │   并 发)     │  │   并 发)     │  │   并 发)     │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                           │
│  两层并发：                                               │
│    外层：ThreadPoolExecutor —— 多个 Agent 同时跑不同行     │
│    内层：Agent 的 tool calling —— 同一行内并发获取信息     │
└───────────────────────────────────────────────────────────┘
```

---

## 迭代路径全览

| 幕 | 阶段 | 方案 | 核心问题 | 结论 |
|------|------|------|---------|------|
| 第一幕 | 1 | LLM + RAG，代码控制管线 | Prompt 的两种失败模式：太简略→输出随意，太详细→LLM 保守刻板 | 转向 Agent 模式 |
| 第二幕 | 1 | Agent 直接读整个 Excel | 几千行数据撑爆上下文 | 放弃 |
| 第二幕 | 2 | Agent 分批读取 | Agent 不知道进度，死循环重读 | 工具必须返回进度信息 |
| 第二幕 | 3-4 | 分批 + 中间件压缩上下文 | Agent 按工具并发而非逐行完成，压缩丢失中间状态（阶段 4 为根因分析） | **放弃** |
| 第二幕 | 5 | 外部批处理 + 行内 Agent 隔离 + 两层并发 | 效果和效率平衡 | **最终方案** |
| 第三幕 | 1 | 类目分级加载（按一级查二级） | 猜错一级无纠错信号，兜底率反升 | 放弃 |
| 第三幕 | 2 | 分类器封装成子 Agent | 丢失检索能力，准度掉了 | 放弃 |
| 第三幕 | 3 | 静态类目表进 system 吃前缀缓存 | 工具返回在对话中部，吃不到缓存全价烧 | **采用** |
| 第三幕 | 4 | 编排层加结果复用缓存（精确→向量→Agent） | 重跑全价烧钱；错误从散布变聚簇 | **采用** |

注：工具默认参数的坑（发现 3）出现在第二幕迭代期间，因属工具设计而非架构方案，不单列一行。
